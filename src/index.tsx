import React from 'react';
import ReactDOM from 'react-dom';

import Dynamo from 'components/dynamo';

window.addEventListener('load', function() {
    ReactDOM.render(React.createElement(Dynamo), document.getElementById('root'));
});
